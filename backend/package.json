{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun",
  "scripts": {
    "build": "cd ../frontend && bun install && vite build --outDir=../backend/frontend/dist"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.65.0",
    "encore.dev": "^1.50.0"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}